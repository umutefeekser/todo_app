const { StyleSheet } = require("react-native")

export default StyleSheet.create({
    container: {
        justifyContent: "center",
        padding: 10,
        borderRadius: 5,
        marginBottom: 10
    }
})